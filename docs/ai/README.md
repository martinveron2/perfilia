# AI and Matching

The AI layer must remain explainable and provider-agnostic.

Initial conceptual pipeline:

`Opportunity → extraction → normalization → requirements → profile evidence → matching → explanation → recommendation`

AI must not invent experience, credentials, achievements, or other facts about a user.

Changes to matching logic should be evaluated against a controlled dataset before release.
